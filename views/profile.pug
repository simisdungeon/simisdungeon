extends default

block header

block content
  div.profile
    div.profile-image(style=`background: url('/images/${carte.imgSrc}') top center no-repeat; background-size: cover;`)
    div.profile-details
      h1.profile-name
        | #{carte.nume}
      h2.profile-tagline
        | #{carte.autor}
      p.profile-bio
        | #{carte.bio}
      h2.profile-tagline
        |  #{carte.review}  
      a.button.button-twitter(href=`https://github.com/lostxap`)
        | GITHUB REPO
    


